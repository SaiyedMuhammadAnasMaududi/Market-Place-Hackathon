import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["www.shutterstock.com","d1csarkz8obe9u.cloudfront.net"], // Allow images from Shutterstock
  },
};

export default nextConfig;
